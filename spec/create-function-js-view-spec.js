'use babel';

import CreateFunctionJsView from '../lib/create-function-js-view';

describe('CreateFunctionJsView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
